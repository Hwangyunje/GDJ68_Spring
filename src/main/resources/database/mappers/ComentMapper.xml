<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.is.main.coment.ComentDAO">
  
  
  <select id="getComentTotal" resultType="Long" parameterType="ComentDTO">
  	SELECT COUNT(COMENTNUM) FROM BOOKCOMENT
  	WHERE BOOKNUM = #{bookNum}
  </select>
  
  <select id="getComentList" resultType="ComentDTO" parameterType="Map">
  	SELECT * FROM
  	(SELECT ROWNUM R,C.* FROM
  	(SELECT * FROM BOOKCOMENTSS
  	WHERE BOOKNUM=#{coment.bookNum}
   ORDER BY COMENTNUM DESC)c)
	WHERE R BETWEEN #{pager.startRow} AND #{pager.lastRow}
  </select>
  
  <insert id="setComentAdd" parameterType="ComentDTO" >
  	INSERT INTO COMENT (COMENTNUM,ID,BOOKNUM,COMENTNAME,COMENTCOMTNETS,COMENTDATE)
  	VALUES
  	(COMENT_SEQ.NEXTVAL,#{id},#{bookNum},#{comentName},#{comentContents},SYSDATE)
  </insert>
  
  </mapper>