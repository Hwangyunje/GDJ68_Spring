<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.is.main.notice.NoticeDAO">
 	
 	 	<select id="getTotal" resultType="Long">
		SELECT COUNT(NOTICENO) FROM NOTICE 	
 	</select>
 	
 <select id="getList" parameterType="com.is.main.util.Pager" resultType="NoticeDTO">
 	SELECT NOTICENO, NOTICESUBJECT, NOTICENAME, NOTICEDATE, NOTICEHIT, NOTICECONTENTS FROM	
		(SELECT ROWNUM R, B.*FROM
			(
			SELECT *FROM NOTICE
			WHERE NOTICENO>0
			ORDER BY NOTICENO DESC
			) B
		)
	WHERE
			R BETWEEN #{startRow} AND #{lastRow}
	</select>
 	 <select id="getDetail" resultType="NoticeDTO" parameterType="NoticeDTO">
 		SELECT * FROM NOTICE WHERE NOTICENO=#{noticeNo}
 	</select>
 	
 	<insert id="setAdd" parameterType="NoticeDTO">
 		INSERT INTO NOTICE 
 		VALUES (BANK_SEQ.NEXTVAL, #{noticeSubject},#{noticeName},SYSDATE,0,#{noticeContent})
 	 </insert>
 	 	
 	<update id="setUpdate" parameterType="NoticeDTO">
 		UPDATE NOTICE SET
 		NOTICESUBJECT=#{noticeSubject},
 		NOTICENAME=#{noticeName},
 		NOTICECONTENTS=#{noticeContent}
 		WHERE NOTICENO=#{noticeNo}
 	</update>
 	 	<delete id="setDelete" parameterType="Long">
 		DELETE NOTICE WHERE NOTICENO=#{No}
 	</delete>
 	  </mapper>