<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.is.main.bankBook.BankBookDAO">
 	<sql id="searchSql">
 	WHERE BOOKNUM>0 
				AND
				<choose>
					<when test="kind == 'name'">
						BOOKNAME LIKE '%||#{search}||%'
					</when>
					<otherwise>
						BOOKCONTENTS LIKE '%||#{search}||%'	
					</otherwise>
				</choose>
				  
 	</sql>
 
 	<select id="getTotal" parameterType="com.is.main.util.Pager" resultType="Long">
		SELECT COUNT(BOOKNUM) FROM BANKBOOK 	
		<include refid="searchSql"></include> 
 	
 	</select>
 	
 	<select id="getList" parameterType="com.is.main.util.Pager" resultType="BankBookDTO">
 	SELECT BOOKNUM, BOOKNAME, BOOKRATE, BOOKSALE, BOOKCONTENTS FROM	
		(SELECT ROWNUM R, B.*FROM
			(
			SELECT *FROM BANKBOOK
			<include refid="searchSql" />				
<!-- 				  <if test="kind == 'name'">
				  AND BOOKNAME LIKE '%검색어%'
				  </if>
				  <if test="kind == 'contents'">
				  AND BOOKCAINTENTS LIKE '%검색어%'
				  </if> -->
			 	  
			ORDER BY BOOKNUM DESC
			) B
		)
	WHERER BETWEEN #{startRow} AND #{lastRow}
	</select>
 	
 	<select id="getDetail" resultType="BankBookDTO" parameterType="BankBookDTO">
 		SELECT *FROM BANKBOOK WHERE BOOKNUM=#{bookNum}
 	</select>
 	
 	<insert id="setAdd" parameterType="BankBookDTO">
 		INSERT INTO BANKBOOK 
 		VALUES (BANK_SEQ.NEXTVAL, #{bookName},#{bookRate},#{bookSale},#{bookContents})
 	</insert>
 	<update id="setUpdate" parameterType="BankBookDTO">
 		UPDATE BANKBOOK SET
 		BOOKNAME=#{bookName},
 		BOOKRATE=#{bookRate},
 		BOOKSALE=#{bookSale},
 		BOOKCONTENTS=#{bookContents}
 		WHERE BOOKNUM=#{bookNum}
 	</update>
 
 	<delete id="setDelete" parameterType="Long">
 		DELETE BANKBOOK WHERE BOOKNUM=#{num}
 	</delete>
 	
  </mapper>