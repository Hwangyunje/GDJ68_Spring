<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.is.main.student.Student.DTO">
  
  	<select id="getList" resultType="com.is.main.student.Student.DTO">
  		SELECT STUDENTNAME, STUDENTNUM, KOR, ENG, MATH, TOTAL, AVG ORDER BY STUDENTNUM DESC
  	</select>
  
  </mapper>